version: "3.8"

services:
  family-dashboard:
    build: .
    container_name: family-dashboard
    restart: unless-stopped
    ports:
      - "3030:80"     # Access at http://YOUR-PI-IP:3030
    # Optional: auto-rebuild on file change when developing
    # volumes:
    #   - ./index.html:/usr/share/nginx/html/index.html:ro
    #   - ./manifest.json:/usr/share/nginx/html/manifest.json:ro
